# define the cron, static, scheduler jobs here
update_random_notification:
  cron: '5 0 * * *'
  class: Scheduler::UpdateRandomNotification
  description: "update everydays random notification with a random time"
  enabled: true